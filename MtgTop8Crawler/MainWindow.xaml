<Window x:Class="MtgTop8Crawler.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:MtgTop8Crawler"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        Title="MtgTop8Crawler by Pierakor"
        Height="300"
        SizeToContent="WidthAndHeight"
        mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBox Name="TextBoxUrl"
                 Grid.Row="0"
                 Grid.Column="0"
                 Margin="5">
            http://mtgtop8.com
        </TextBox>
        <Button Name="StartStopButton"
            Grid.Row="0"
                Grid.Column="1"
                Margin="5"
                Click="Button_Click">
            Start the Crawl
        </Button>
        <Label Name="LabelLog"
               Grid.Row="1"
               Grid.Column="0"
               Grid.ColumnSpan="2"
               Margin="5" />
    </Grid>
</Window>
